const BASE_API_ORIGIN = 'https://open-attendance.atma-dev.com' as const
const BASE_API = `${BASE_API_ORIGIN}/api/` as const

const CONSTS = {
  URL: {
    BASE_API,
    BASE_API_ORIGIN,
  },
}

export default CONSTS
